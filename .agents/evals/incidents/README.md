# Agent Incidents

Store only real, observed agent failures here. Do not create fictional incidents to populate the directory.

Incident records are for diagnosis and improvement, not normal runtime context.

## Required Follow-up

A repeatable incident should normally produce at least one regression evaluation. The incident explains what happened and why; the evaluation defines behavior that must remain correct in the future.

Use `_TEMPLATE.md` for new records.
